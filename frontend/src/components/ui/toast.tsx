/**
 * Toast component - re-exports from sonner.
 *
 * shadcn/ui v4 replaced the old toast component with sonner.
 * This module provides a convenient re-export so consumers
 * can import from "@/components/ui/toast".
 */
export { Toaster } from "./sonner"
export { toast } from "sonner"
